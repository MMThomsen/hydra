================================================================================
CORRECTED ANALYSIS: Propositional vs First-Order Temporal
================================================================================

Original (propositional): p0 OR (▷ [2,2] p1* p2)
With propositional log:
@0 p1 p2
@0 p0 p2
@1 p1
@4 p0 p2
@4 p1 p2
@6 p0 p1 p2
@7 p1

Result: 0:0 false, 0:1 true, 1:0 false, 4:0 true, 4:1 true

New (first-order): p0(x) OR (▷ [2,2] p1(x)* p2(x))
With first-order log:
@0 p1(Alice) p2(Alice)
@0 p0(Bob) p2(Charlie)
@1 p1(Dave)
@4 p0(Alice) p2(Alice)
@4 p1(Bob) p2(Bob)
@6 p0(Charlie) p1(Alice) p2(Dave)
@7 p1(Eve)

Expected result: 0:0 false, 0:1 true, 1:0 false, 4:0 true, 4:1 ?

================================================================================
DETAILED EVALUATION
================================================================================

Formula: p0(x) OR (▷ [2,2] p1(x)* p2(x))

Key insight: Each event is evaluated INDEPENDENTLY with its own set of predicates.

────────────────────────────────────────────────────
[0:0] @0 {p1(Alice), p2(Alice)}
────────────────────────────────────────────────────
├─ p0(x): No p0 in event → Leaf(FALSE)
└─ ▷ [2,2] p1(x)* p2(x): Look at @2 (no such timestamp) → Leaf(FALSE)
Result: FALSE OR FALSE = FALSE
Expected: 0:0 false ✓

────────────────────────────────────────────────────
[0:1] @0 {p0(Bob), p2(Charlie)}
────────────────────────────────────────────────────
├─ p0(x): p0(Bob) → Node("x", [({Bob}, Leaf(TRUE))])
└─ ▷ [2,2] p1(x)* p2(x): Look at @2 (no such timestamp) → Leaf(FALSE)
Result: (Bob→TRUE) OR FALSE = (Bob→TRUE)
has_true_leaf = TRUE
Expected: 0:1 true ✓

────────────────────────────────────────────────────
[1:0] @1 {p1(Dave)}
────────────────────────────────────────────────────
├─ p0(x): No p0 → Leaf(FALSE)
└─ ▷ [2,2] p1(x)* p2(x): Look at @3 (no such timestamp) → Leaf(FALSE)
Result: FALSE OR FALSE = FALSE
Expected: 1:0 false ✓

────────────────────────────────────────────────────
[4:0] @4 {p0(Alice), p2(Alice)}
────────────────────────────────────────────────────
├─ p0(x): p0(Alice) → Node("x", [({Alice}, Leaf(TRUE))])
└─ ▷ [2,2] p1(x)* p2(x): Look at @6...
Result: (Alice→TRUE) OR (...) = TRUE (short-circuit on left side)
has_true_leaf = TRUE
Expected: 4:0 true ✓

────────────────────────────────────────────────────
[4:1] @4 {p1(Bob), p2(Bob)}
────────────────────────────────────────────────────
├─ p0(x): No p0 → Leaf(FALSE)
│
└─ ▷ [2,2] p1(x)* p2(x): Look at @6 (exists!)
   At @6: {p0(Charlie), p1(Alice), p2(Dave)}
   
   Evaluate UNTIL: p1(x)* p2(x) at @6
   ├─ p1(x): p1(Alice) → Node("x", [({Alice}, Leaf(TRUE))])
   ├─ p2(x): p2(Dave) → Node("x", [({Dave}, Leaf(TRUE))])
   
   UNTIL at single timestamp:
   For "p1* p2" at a single timestamp, we check:
   - Does p2 hold? YES (Dave→TRUE)
   - UntilMonitor returns TRUE when p2 is satisfied
   
   But the variables are DIFFERENT (Alice vs Dave)!
   
   Let me check propositional case:
   @4 {p1, p2} with formula (▷ [2,2] p1* p2)
   Look at @6: {p0, p1, p2}
   p1* p2: Both p1 and p2 are TRUE (propositionally)
   Result: TRUE
   
   For first-order with PDT:
   p1(x): {x=Alice}
   p2(x): {x=Dave}
   
   When UNTIL combines these PDTs:
   - We need x such that p1(x)* p2(x) holds
   - p1(Alice) is TRUE, p2(Alice) is FALSE
   - p1(Dave) is FALSE, p2(Dave) is TRUE
   - No common x satisfies both!
   
   But wait - UNTIL semantics at a single timestamp:
   "p1* p2" means "p2 holds now OR (p1 holds and p1* p2 holds later)"
   At a single timestamp with no future, it just checks if p2 holds.
   
   If the monitor returns has_true_leaf(p2.b), it returns TRUE
   because p2(Dave) is TRUE.
   
   But this seems wrong for the variable binding!
   The formula is p1(x)* p2(x) - same variable x.
   We need x such that: p2(x) holds (or p1(x) holds continuously until p2(x))
   
   At @6:
   - x=Alice: p1(Alice)=TRUE, p2(Alice)=FALSE → UNTIL not satisfied
   - x=Dave: p1(Dave)=FALSE, p2(Dave)=TRUE → p2 holds immediately, UNTIL satisfied!
   
   So the result should be: Node("x", [({Dave}, Leaf(TRUE))])
   has_true_leaf = TRUE
   
Result: FALSE OR TRUE = TRUE
Expected: 4:1 true ✓

────────────────────────────────────────────────────
[6:0] @6 {p0(Charlie), p1(Alice), p2(Dave)}
────────────────────────────────────────────────────
├─ p0(x): p0(Charlie) → Node("x", [({Charlie}, Leaf(TRUE))])
└─ ▷ [2,2] p1(x)* p2(x): Look at @8 (no such timestamp) → Leaf(FALSE)
Result: (Charlie→TRUE) OR FALSE = (Charlie→TRUE)
has_true_leaf = TRUE
Expected: 6:0 true ✓

────────────────────────────────────────────────────
[7:0] @7 {p1(Eve)}
────────────────────────────────────────────────────
├─ p0(x): No p0 → Leaf(FALSE)
└─ ▷ [2,2] p1(x)* p2(x): Look at @9 (no such timestamp) → Leaf(FALSE)
Result: FALSE OR FALSE = FALSE
Expected: 7:0 false ✓

================================================================================
EXPECTED RESULTS SUMMARY
================================================================================

0:0 false
0:1 true
1:0 false
4:0 true
4:1 true
6:0 true
7:0 false

================================================================================
ISSUE WITH CURRENT PDT IMPLEMENTATION
================================================================================

The problem is likely in how UntilMonitor handles PDTs.

Looking at monitor.cpp lines 186-188:
```cpp
if ((has_true_leaf(z->second.second) ? TRUE : FALSE) && memL(...))
    return BooleanVerdict(back->ts, Pdt::Leaf(TRUE), vars);
else if ((!has_true_leaf(z->second.first)) ? TRUE : FALSE)
    return BooleanVerdict(back->ts, Pdt::Leaf(FALSE), vars);
else return BooleanVerdict(back->ts, Pdt::Leaf(FALSE), vars);
```

This collapses the PDT to a scalar TRUE/FALSE!

The UNTIL operator should:
1. Evaluate p1 and p2, getting PDTs
2. Apply UNTIL logic to combine the PDTs properly
3. Return a PDT that represents which variable assignments satisfy the UNTIL

Currently it's just checking has_true_leaf and returning Leaf(TRUE) or Leaf(FALSE),
which loses all the variable binding information!

This is the BUG!
